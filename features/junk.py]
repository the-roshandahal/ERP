models.py
class CustomUser(AbstractUser):
    address = models.CharField(max_length=100)

in settings.py
AUTH_USER_MODEL = 'users.CustomUser'

in templates
<p>User address: {{ request.user.address }}</p>